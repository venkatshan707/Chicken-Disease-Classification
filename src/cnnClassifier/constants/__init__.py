from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml") #firstly reading config.yaml which contains config info and training data images
PARAMS_FILE_PATH = Path("params.yaml")#secondly reading config.yaml

# The file paths are constant, thats why we have created one folder called constant and mentioning paths here